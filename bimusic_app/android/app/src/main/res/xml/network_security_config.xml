<?xml version="1.0" encoding="utf-8"?>
<!-- Allow cleartext HTTP for self-hosted backends on LAN (user-configured IP, not predictable) -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
